name: Retired BALI STOCK GitHub Pages

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub Pages channel retired
        run: |
          echo "BALI STOCK iPhone production is hosted by the stable Supabase runtime."
          echo "GitHub Pages is intentionally retired and must not publish the application."
